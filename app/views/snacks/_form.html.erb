<%= form_for(@snack) do |f| %>
  <% if @snack.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@snack.errors.count, "error") %> prohibited this snack from being saved:</h2>

      <ul>
      <% @snack.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    Nombre: <%= f.text_field :name %>
  </div>
  <div class="field">
    Calorías: <%= f.number_field :calories %>
  </div>

  <div class="hidden"><%= f.check_box :approved %></div>

  <div class="spacer">
    <h2>Ejemplo</h2>
    Nombre: <b>Snickers</b><br>
    Calorías: <b>181</b><br>
  </div>

  <div class="actions spacer">
    <%= f.submit %>
  </div>
<% end %>
